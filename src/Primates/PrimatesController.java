package Primates;

/**
 * Represents a Controller for Primates GUI:
 */
public interface PrimatesController {

    /**
     * Execute the Primates GUI.
     * Preset to add the eight species to the combobox for user to select
     * Preset to add the species of the monkey to the combobox for user to select when creating a monkey
     * Preset to add the sex of the monkey to the combobox for user to select when creating a monkey
     * Preset to add the favourite food of the monkey to the combobox for user to select when creating a monkey
     */
    void playGame();
}
